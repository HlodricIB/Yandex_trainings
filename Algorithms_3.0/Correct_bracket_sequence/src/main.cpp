#include <iostream>
#include <stack>
#include <unordered_map>

int main()
{
    std::stack<char> s;
    std::unordered_map<char, int> m;
    char c;
    std::cin >> c;
    while (c != '\n')
    {
        s.push(c);
        std::cin >> c;
    }
    bool if_continue = true;
    while (true)
    {

    }


    return 0;
}
